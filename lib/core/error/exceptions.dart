/// Enum containing all exceptions that can be thrown by the application.
enum AppExceptions {
  /// Thrown when there are issues with user authentication,
  /// such as invalid credentials or expired sessions.
  authException,
}
